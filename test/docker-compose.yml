version: '2'

services:

    tests:
        image: 'phpunit/phpunit:latest'
        entrypoint:
            - /bin/sh
            - -c
        command:
            - 'phpunit test/*.php'
        volumes:
            - '../:/app'
